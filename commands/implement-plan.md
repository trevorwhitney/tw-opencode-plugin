---
description: "Execute an implementation plan with subagent-driven-development, dispatching Sonnet implementer subagents"
---
Invoke the subagent-driven-development skill and follow it exactly.
Dispatch all subagents using the "implementer" agent type instead of "general".

$ARGUMENTS
