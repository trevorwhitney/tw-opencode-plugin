{
  "name": "tw-opencode-plugin",
  "version": "0.1.0",
  "description": "Personal OpenCode plugin — central home for skills, commands, and custom tools",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bash scripts/build.sh",
    "typecheck": "tsc --noEmit",
    "deploy": "bash scripts/deploy.sh",
    "dev": "bash scripts/build.sh && tsc --watch",
    "prepare": "husky",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "plugin"
  ],
  "dependencies": {
    "@opencode-ai/plugin": "^1.2.16"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "husky": "^9.1.7",
    "typescript": "^5.8.2",
    "vitest": "^4.0.18"
  }
}
